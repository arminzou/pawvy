{
    "name": "pawvy",
    "private": true,
    "version": "0.1.0",
    "scripts": {
        "dev": "concurrently -n backend,frontend -c blue,green \"PORT=3001 pnpm -C backend dev\" \"pnpm -C frontend dev\"",
        "dev:frontend": "pnpm -C frontend dev",
        "dev:backend": "PORT=3001 pnpm -C backend dev",
        "stop": "lsof -ti :3001,5173 | xargs kill -9 2>/dev/null || true",
        "init": "pnpm install && pnpm -C backend install && pnpm -C backend init-db && pnpm -C frontend install",
        "build": "pnpm -C frontend build",
        "start": "pnpm -C backend start",
        "start:prod": "pnpm run build && pnpm run start",
        "sync-docs": "pnpm -C backend sync-docs",
        "ingest-sessions": "pnpm -C backend ingest-sessions",
        "test:e2e": "pnpm playwright test",
        "test": "pnpm vitest run -c vitest.hooks.config.ts",
        "test:backend": "cd backend && pnpm vitest run",
        "test:frontend": "cd frontend && pnpm vitest run",
        "test:hooks": "pnpm vitest run -c vitest.hooks.config.ts"
    },
    "packageManager": "pnpm@10.29.2",
    "devDependencies": {
        "@playwright/test": "^1.54.2",
        "@types/node": "^25.3.0",
        "concurrently": "^9.2.0",
        "dotenv": "^17.3.1",
        "vitest": "^3.2.4"
    }
}