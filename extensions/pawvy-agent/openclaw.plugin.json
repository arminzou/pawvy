{
  "id": "pawvy-agent",
  "name": "Pawvy Agent Tracker",
  "description": "Tracks agent lifecycle events and reports to Pawvy for real-time agent display",
  "version": "0.1.0",
  "openclaw": {
    "extensions": ["./index.ts"]
  },
  "configSchema": {
    "type": "object",
    "properties": {
      "webhookUrl": {
        "type": "string",
        "description": "Pawvy webhook URL for agent status updates"
      },
      "idleTimeoutMs": {
        "type": "number",
        "description": "Milliseconds to wait after agent_end before returning to idle",
        "default": 30000
      }
    }
  },
  "uiHints": {
    "webhookUrl": {
      "label": "Webhook URL",
      "placeholder": "http://localhost:3001/api/webhook/pawvy"
    },
    "idleTimeoutMs": {
      "label": "Idle Timeout (ms)",
      "placeholder": "30000"
    }
  }
}
